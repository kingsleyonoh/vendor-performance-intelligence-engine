# Example Pattern — Delete Me

> Template shape for a pattern file. Delete this file once a real pattern is documented.

## Purpose

One sentence. What this pattern does and why.

## When to use

- Bullet list of conditions / surfaces where this pattern applies.
- Call out the failure mode the pattern prevents.

## How it works

- Steps, invariants, or the contract (e.g. "reserve → finalize → abort → reaper").
- Keep it concrete enough that an implementer can apply it without re-reading the originating batch.

## Example

Short code snippet or pseudo-code illustrating the pattern.

## Cross-references

- Originating batch / PRD section (if applicable).
- Related patterns: `.agent/knowledge/patterns/NNN-other-pattern.md`
- Sibling foundation primitives: `.agent/knowledge/foundation/*.md`
