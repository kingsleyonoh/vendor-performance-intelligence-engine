# Example Module — Delete Me

> Template shape for a module file. Delete this file once a real module is documented.

## Purpose

One sentence. What this module is responsible for.

## Key files

- `src/<module>/index.ts` — entry point / public API
- `src/<module>/<file>.ts` — internal implementation
- `src/<module>/README.md` — if the module has its own docs

## Dependencies

- Upstream (this module imports): list modules / external libs this module depends on.
- Downstream (imports this module): which other modules use this one.

## Tests

- `tests/<module>/` — where tests live.
- Notable invariants the tests enforce.

## Cross-references

- Related foundation primitives: `.agent/knowledge/foundation/*.md`
- Relevant patterns: `.agent/knowledge/patterns/*.md`
